<%= form_for(qualification) do |f| %>
  <% if qualification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qualification.errors.count, "error") %> prohibited this qualification from being saved:</h2>

      <ul>
      <% qualification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_id %>
    <!-- <%= f.number_field :client_id %>-->
    <%= select('qualification', 'client_id',
                                 Client.order(:name){|p|
                                   p
                                 }
    ) %>
  </div>

  <div class="field">
    <%= f.label :restaurant_id %>
    <%= f.collection_select(:restaurant_id, Restaurant.order(:name), :id, :name, :prompt => true)%>
  </div>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_field :note %>
  </div>

  <div class="field">
    <%= f.label :amount_spent %>
    <%= f.text_field :amount_spent %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
